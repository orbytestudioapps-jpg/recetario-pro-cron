name: Provider OCR Jobs (Recetario PRO)

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  process_provider_jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Run process-provider-jobs via CURL (Service Role)
        run: |
          echo "🚀 Llamando a process-provider-jobs con CURL..."
          curl -X POST \
            "https://gfgbyhcgemictofcdokd.supabase.co/functions/v1/process-provider-jobs" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
            -d '{}'
        env:
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}



